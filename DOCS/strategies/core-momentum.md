# Core — Momentum

## Source (doc legacy)

`DOCS/legacy/Aurora_Documentation.md` (section “Core Momentum”).

## Résumé

Composant :
- `AuKeltnerKama` (canal dynamique KAMA + ATR) + ER.

Paramètres :
- `InpKeltner_*` + (optionnel) `InpSmartMom_*` pour des canaux dynamiques.

## Inputs

Voir `../inputs/1-3-momentum.md`.

